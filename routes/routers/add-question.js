var express = require('express');
var router = express.Router();

router.post('/getCource', function(req, res, next) {
  var result = req.body;
  console.log('result=' + result.cource);
  if (type == '填空题') {

  }
});
module.exports = router;
